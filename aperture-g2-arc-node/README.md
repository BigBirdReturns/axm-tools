# AXM Aperture G2 Arc Node closure

This temporary transport reconstructs the exact Linux x64 Node dependency tree required to replay the landed Arc timed-media provider contract locally.

It checks out Arc landed commit `c8b78629217b9ba7237c1ecfff47cdc0e28cbf69`, proves qualified feature head `a94fcf69898c7d5ceb40b733f0ca5e6a42794623` is its ancestor, installs the exact `package-lock.json` through `npm ci` under Node `22.16.0`, runs strict TypeScript and the focused canonical-story digest and timed-media tests, then packages `node_modules` deterministically with complete receipts and checksums.

The branch and pull request must never merge. The artifact carries dependency bytes and qualification evidence only. It does not acquire Arc narrative authority, AXM Aperture product authority, provider identity, viewer state, playback control, or G2 acceptance.

The control question is whether the landed Arc contract can be replayed from its exact lockfile and dependency bytes without allowing a temporary dependency carrier to become source or gate authority.
