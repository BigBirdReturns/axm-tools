# Synthetic sample pack

These files contain no real patient, sponsor, site, drug, or protocol data.

Load all files together to exercise the complete Clinical Site Fabric v0.2.0 path. The tool also embeds byte-independent copies of the same synthetic records behind **Load complete synthetic sample pack** so the interface remains functional from a single HTML file.

The Markdown certificate is intentionally model-relative and cannot set clinical disposition.
