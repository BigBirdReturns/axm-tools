SCG — LinkedIn / identity export set  (Identity System v3 — the LIVE mark)
Source of truth: scg-identity/SCG Identity System v3.html  →  scg-pixel-mark.js (SCGPX)
The mark is a HAND-SET 16x18 pixel dandelion, mid-dispersal. Never redrawn, never
centered, never tidied. This is the mark already live in the axm.tools footer.

PALETTE (4 tokens)
  Bone   #ECE7D8   the bloom · text on void
  Olive  #7C7F57   stem · secondary labels
  Void   #0D0C09   the field · all backgrounds
  Ember  #C24B2C   findings / flags ONLY — never in the mark
TYPE
  IBM Plex Sans 600  — display & lockups (uppercase, wide tracking)
  IBM Plex Mono 400  — body, labels, evidence

FILES
  SCG-avatar-800.png                 DEFAULT LinkedIn / social avatar. Void field, mark
                                     offset right-of-center per the rule. 400x400 export at 2x.
  SCG-mark-on-bone-800.png           Inverted for light / print (doc headers): dark bloom,
                                     olive stem, on bone. Bloom stays void-dark.
  SCG-favicon-256/128/64/48/32.png   Favicon ramp. Integer cell sizes only — same input,
                                     same pixels, every render. Reads at 32px.
  SCG-linkedin-cover-desktop-2256x382.png      Company cover (1128x191 ratio @2x): name left,
                                               tagline in olive mono, mark hard right.
  SCG-linkedin-cover-mobile-safe-2256x382.png  Same lockup pulled to center — survives
                                               LinkedIn's mobile center-crop.

Lead with the dark avatar. Keep ember out of the mark. Upload covers at 2256x382.
