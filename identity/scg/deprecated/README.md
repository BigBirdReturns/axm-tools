# DEPRECATED — do not resurrect

## The sterile vector dandelion
- **What:** a smooth **vector** dandelion — concentric rings of solid circles, ink/paper/**rust** palette, Archivo type, resolved and symmetric.
- **Where it came from (historical):** `scg-identity/SCG Mature Identity.html` → `buildDandelionSVG()`.
- **Status:** **REJECTED** by `../SCG_MARK_CONSTITUTION.md` §7.

## Why it is out of custody
- No coordinate map — it cannot be audited cell-by-cell.
- Puts **rust inside the mark** (constitution §4: ember is findings/flags only, never the mark).
- Reads as a generic consultancy icon; breaks the pixel lineage.

**It is not an alternate direction.** The image assets are intentionally **not** stored here so a future pass cannot grab them by convenience. If you find a rust/vector dandelion anywhere in the tree, it is out of custody — replace it with the pixel derivative in `../source/scg-pixel-mark.js`.
