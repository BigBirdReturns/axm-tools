document.getElementById('copy').addEventListener('click',async()=>{const prompt=document.getElementById('prompt');const status=document.getElementById('copy-status');try{await navigator.clipboard.writeText(prompt.value);status.textContent='Copied. Paste it into your existing Hronaut agent.';}catch{prompt.focus();prompt.select();status.textContent='Clipboard access was denied. The prompt is selected; copy it with your keyboard.';}});
