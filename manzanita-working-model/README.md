# Manzanita Works Working Model

Public route: `https://bigbirdreturns.github.io/axm-tools/manzanita-working-model/`

The working model begins with one concrete problem and carries it through seven operating stages: signal, source, authority, safe action, fallback, closure, and learning. Four representative cases demonstrate the same mechanism across wildfire assistance, tools and time, mobility, and continuity.

The public page translates those mechanics into receiver language. Its seven visible steps are Need, Evidence, Decision owner, Safe next step, Backup path, Outcome, and Next improvement. Stable machine-readable identifiers remain available in the contracts and exported packet rather than occupying the visible explanation. Browser campaigns verify every visible case, its exported identities, the absence of prohibited internal phrasing, and compact prompts at 320 pixels with 200 percent text.

The first view presents one four-row case state instead of duplicating the complete seven-stage sequence. The shared-resources instrument names six resource types without inventing utilization, readiness, or capacity values. The page then exposes four separately governed working parts, three operating safeguards, and five facts required for a pilot proposal.

The local export preserves unresolved values and remains either `INCOMPLETE_PREPARATION_HELD` or `PREPARED_FOR_ACCOUNTABLE_REVIEW_NOT_ACCEPTED`. Publication proves the route and qualified bytes only. It creates no institutional adoption, participant consent, field authority, spending authority, assignment, representation, work order, eligibility decision, or program effect.

The release contract enforces six current public files, zero rendered photography, zero dead visual assets, zero synthetic capacity metrics, zero untranslated public jargon from the prohibited list, an 84 CSS-pixel desktop hero ceiling, an 11 CSS-pixel visible-text floor, a 44-pixel interactive-control floor, bounded document heights, zero horizontal overflow, exact anchor clearance, and desktop, mobile, and 320-pixel-at-200-percent-text browser qualification. An inline empty favicon prevents an unowned origin request from contaminating browser evidence.
