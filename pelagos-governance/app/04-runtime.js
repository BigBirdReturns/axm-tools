'use strict';
function renderHandoff(){return viewHead('Handoff','Verify custody, preserve the baseline, export bounded projections, recover from device loss, and test a cold successor.')+`<div class="grid two"><section class="card"><div class="card-head"><h3>Integrity and succession</h3></div><div class="card-body"><div id="healthBox" class="callout">Computing runtime health…</div><div style="margin-top:12px"><button class="btn" id="verifyLedger">Verify receipt chain</button> <button class="btn" id="coldReplay">Run cold replay</button></div><div id="handoffResult"></div></div></section><section class="card"><div class="card-head"><h3>Portable exports</h3></div><div class="card-body grid"><button class="btn" data-export="full">Full workspace JSON</button><button class="btn" data-export="public">Public-safe projection</button><button class="btn" data-export="diligence">Diligence projection</button><button class="btn" data-export="counsel">Counsel residue</button><button class="btn" data-export="claims">Claims CSV</button><button class="btn" data-export="instruments">Instruments CSV</button><button class="btn" data-export="offline">Offline application</button></div></section></div><div class="grid two" style="margin-top:14px"><section class="card"><div class="card-head"><h3>Encrypted backup and restore</h3></div><div class="card-body"><div class="field"><label for="backupPass">Passphrase</label><input id="backupPass" type="password" autocomplete="new-password"></div><div style="margin-top:10px"><button class="btn safe" id="encryptBackup">Create encrypted backup</button> <button class="btn" id="restoreBackup">Restore encrypted backup</button></div><p class="muted">PBKDF2-SHA-256, 250,000 iterations, AES-GCM-256. The passphrase never leaves this page.</p></div></section><section class="card"><div class="card-head"><h3>Workspace control</h3></div><div class="card-body"><button class="btn" id="importWorkspace">Import full workspace</button> <button class="btn danger" id="resetWorkspace">Reset local workspace</button><p class="muted">Import validates before replacement and retains an interruption journal. Reset does not alter the embedded public cartridge.</p></div></section></div>`}
function renderLineage(){return viewHead('Lineage','The mechanisms retained from the twelve published AXM surfaces and the staged source-continuity successor. No runtime code is shared.')+`<div class="record-list">${PUBLIC.lineage.map((x,i)=>`<article class="record"><div class="id">L-${String(i+1).padStart(2,'0')}</div><h3>${esc(x.source)}</h3><div class="facts"><div class="fact"><b>Lesson retained</b><span>${esc(x.lesson)}</span></div><div class="fact"><b>Carried forward here</b><span>${esc(x.carried_forward)}</span></div></div></article>`).join('')}</div>`}
function render(bind=true){renderNav();renderStatus();let html='';if(activeView==='today')html=renderToday();if(activeView==='docket')html=renderDocket();if(activeView==='claims')html=renderClaims();if(activeView==='instruments')html=renderInstruments();if(activeView==='evidence')html=renderEvidence();if(activeView==='decisions')html=renderDecisions();if(activeView==='stress')html=renderStress();if(activeView==='handoff')html=renderHandoff();if(activeView==='lineage')html=renderLineage();$('#view').innerHTML=html;$('#footer').textContent=`${RELEASE} · ${PUBLIC.meta.status} · public ${publicDigest} · zero external-effect adapters`;bindView();if(bind)$('#main').focus({preventScroll:true})}
function bindView(){bindSearch();$$('[data-sub]').forEach(b=>b.onclick=()=>{subview[activeView]=b.dataset.sub;render()});const ar=$('#addIntake');if(ar)ar.onclick=openIntakeDialog;$$('[data-open-decision]').forEach(b=>b.onclick=()=>openDecisionDialog(b.dataset.openDecision));$$('[data-correct-claim]').forEach(b=>b.onclick=()=>openCorrectionDialog(b.dataset.correctClaim));$$('[data-plan-claim]').forEach(b=>b.onclick=()=>{activeView='instruments';subview.instruments='plans';currentSearch=b.dataset.planClaim;render()});const risk=$('#claimRisk');if(risk)risk.onchange=()=>{$$('#claimList .record').forEach(x=>x.classList.toggle('hidden',!!risk.value&&x.dataset.risk!==risk.value))};const choose=$('#chooseSources'),dropBtn=$('#dropButton');if(choose)choose.onclick=()=>$('#sourceFiles').click();if(dropBtn)dropBtn.onclick=()=>$('#sourceFiles').click();const dz=$('#dropZone');if(dz){['dragenter','dragover'].forEach(e=>dz.addEventListener(e,x=>{x.preventDefault();dz.classList.add('drag')}));['dragleave','drop'].forEach(e=>dz.addEventListener(e,x=>{x.preventDefault();dz.classList.remove('drag')}));dz.addEventListener('drop',e=>admitFiles([...e.dataTransfer.files]))}const rs=$('#runStress');if(rs)rs.onclick=runStress;const vl=$('#verifyLedger');if(vl)vl.onclick=async()=>{$('#handoffResult').innerHTML=`<div class="receipt">${esc(JSON.stringify(await verifyReceipts(),null,2))}</div>`};const cr=$('#coldReplay');if(cr)cr.onclick=async()=>{const r=await coldReplay();$('#handoffResult').innerHTML=`<div class="callout ${r.verdict==='PASS'?'good':'stop'}"><b>${r.verdict}</b> · ${r.passed}/${r.total} checks</div>${r.checks.map(x=>`<div class="result"><div>${severityTag(x[1]?'PASS':'HOLD')}</div><div>${esc(x[0])}</div></div>`).join('')}`};if($('#healthBox')){const h=runtimeHealth();$('#healthBox').className=`callout ${h.unexpected_resources.length?'stop':'good'}`;$('#healthBox').innerHTML=`<b>${h.unexpected_resources.length?'Unexpected runtime requests detected':'Runtime boundary intact'}</b><br>Web Crypto ${h.crypto?'yes':'no'} · IndexedDB ${h.indexeddb?'yes':'no'} · File API ${h.file_api?'yes':'no'} · dialog ${h.dialog?'yes':'no'} · requests ${h.unexpected_resources.length}`}
$$('[data-export]').forEach(b=>b.onclick=async()=>{const k=b.dataset.export;if(k==='full'){await appendReceipt('workspace_exported','workspace',{projection:'full'});download('DDV-PEL-002-full-workspace.json',JSON.stringify(workspace,null,2))}if(k==='public')download('DDV-PEL-002-public-projection.json',JSON.stringify(publicProjection(),null,2));if(k==='diligence')download('DDV-PEL-002-diligence-projection.json',JSON.stringify(diligenceProjection(),null,2));if(k==='counsel')download('DDV-PEL-002-counsel-residue.json',JSON.stringify(counselResidue(),null,2));if(k==='claims')download('DDV-PEL-002-claims.csv',csv(PUBLIC.claims),'text/csv');if(k==='instruments')download('DDV-PEL-002-instruments.csv',csv(PUBLIC.instruments),'text/csv');if(k==='offline')download('DDV-PEL-002-Pelagos-Governance-Layer.html','<!doctype html>\n'+document.documentElement.outerHTML,'text/html')});if($('#encryptBackup'))$('#encryptBackup').onclick=async()=>{try{const env=await encryptWorkspace($('#backupPass').value);download('DDV-PEL-002-encrypted-backup.json',JSON.stringify(env,null,2));toast('Encrypted backup created')}catch(e){toast(e.message)}};if($('#restoreBackup'))$('#restoreBackup').onclick=()=>$('#restoreFile').click();if($('#importWorkspace'))$('#importWorkspace').onclick=()=>$('#importFile').click();if($('#resetWorkspace'))$('#resetWorkspace').onclick=()=>{if(confirm('Reset local workspace? The public cartridge remains.'))resetWorkspace()}}
function openDialog(title,body,actions){$('#dialogTitle').textContent=title;$('#dialogBody').innerHTML=body;$('#dialogActions').innerHTML=actions;$('#formDialog').showModal();$$('[data-close-dialog]').forEach(b=>b.onclick=()=>$('#formDialog').close())}
function openIntakeDialog(){openDialog('Add local inbound',`<form class="form" id="intakeForm"><div class="field"><label>Object type</label><select name="object_type">${PUBLIC.lists.object_type.map(x=>`<option>${esc(x)}</option>`).join('')}</select></div><div class="field"><label>Counterparty</label><input name="counterparty" required></div><div class="field wide"><label>Summary / decision</label><textarea name="summary" required></textarea></div><div class="field"><label>Offered value</label><textarea name="offered"></textarea></div><div class="field"><label>Requested value</label><textarea name="requested"></textarea></div><div class="field"><label>Instrument / source</label><input name="instrument"></div><div class="field"><label>Visibility</label><select name="visibility"><option value="confidential">Confidential</option><option value="privileged">Privileged / counsel</option><option value="internal">Internal</option><option value="public">Public</option></select></div><div class="field"><label>Owner</label><select name="owner">${PUBLIC.lists.owner.map(x=>`<option>${esc(x)}</option>`).join('')}</select></div><div class="field"><label>Authority source</label><input name="authority"></div><div class="field"><label>Due</label><input name="due" type="date"></div></form>`,`<button class="btn" data-close-dialog>Cancel</button><button class="btn primary" id="saveIntake">Save local record</button>`);$('#saveIntake').onclick=async()=>{const f=new FormData($('#intakeForm'));const rec={id:`INT-LOCAL-${String(workspace.intake.length+1).padStart(4,'0')}`,at:now(),object_type:f.get('object_type'),counterparty:f.get('counterparty'),summary:f.get('summary'),offered:f.get('offered'),requested:f.get('requested'),instrument:f.get('instrument'),visibility:f.get('visibility'),owner:f.get('owner'),authority:f.get('authority'),due:f.get('due'),state:'Discovered',external_effect:'held',source_receipt_ids:[],private_body:true};if(!rec.counterparty||!rec.summary){toast('Counterparty and summary required');return}workspace.intake.push(rec);await appendReceipt('intake_recorded',rec.id,{object_type:rec.object_type,counterparty:rec.counterparty,visibility:rec.visibility,external_effect:'held'});$('#formDialog').close();render()}}
