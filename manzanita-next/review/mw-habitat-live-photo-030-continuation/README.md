# Useful Plant v30 bounded source-replay review

This draft-review object carries a 71-file text source carrier as one verified XZ payload split across eight fixed text parts. The parts are concatenated and decoded only inside the workflow runner, where the complete 95-check continuation campaign, runtime assembly witness, runtime reseal, and fourteen-criterion operator review are replayed. The measured runtime output is uploaded as an Actions artifact.

The payload is compressed and partitioned because the available repository connector cannot transport the locally rendered binary carrier without converting a review operation into an uncontrolled binary write. The local exact rendered candidate remains sealed at `317d6e3017ddc4e5696eb68a733b556bf0f7f0c49608213d0544ada05aec63ea`; the local complete review carrier remains sealed at `fd71d2e83517c9e767bc301f64aa958bc2972b6cb3a28e21258c1496de58d50f`. Neither hash is claimed for workflow-generated output.

The changed-path boundary admits only this review directory and its review workflow. `manzanita/`, root entry points, GitHub Pages, merge authority, exact parent and Plant media, operator acceptance, and release remain untouched.
