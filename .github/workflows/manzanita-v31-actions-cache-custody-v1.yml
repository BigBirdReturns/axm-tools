name: Manzanita V31 Actions cache custody inventory

on:
  workflow_dispatch: {}

permissions:
  actions: read
  contents: read

concurrency:
  group: manzanita-v31-actions-cache-custody-v1
  cancel-in-progress: false

jobs:
  closure:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve the completed bounded campaign boundary
        shell: bash
        run: |
          set -euo pipefail
          cat <<'EOF'
          The bounded repository Actions cache inventory completed in run 33689171382.
          Result: PASS_NO_ACTIONS_CACHES_RETAINED.
          Repository caches observed: 0.
          Candidate production-input caches observed: 0.
          This compatibility entrypoint is manual-only and creates no production, merge, release, route, Pages, or external-effect authority.
          V14 remains the controlling governed HOLD.
          EOF
