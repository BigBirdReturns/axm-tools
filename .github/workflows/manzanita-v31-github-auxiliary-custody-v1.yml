name: Manzanita V31 GitHub auxiliary custody

on:
  workflow_dispatch: {}

permissions:
  actions: read
  contents: read
  issues: read
  pull-requests: read

concurrency:
  group: manzanita-v31-github-auxiliary-custody-v1
  cancel-in-progress: false

jobs:
  closure:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve the completed bounded campaign boundary
        shell: bash
        run: |
          set -euo pipefail
          cat <<'EOF'
          The bounded GitHub auxiliary custody campaign completed in run 33691037897.
          Result: PASS_GITHUB_AUXILIARY_CUSTODY_SURFACES_SCANNED_NO_NEW_EXACT_V31_BYTES.
          The authenticated wiki probe established that the repository has no wiki Git repository.
          The issue, pull-request, comment, and release-body graph contained no recognized GitHub-hosted attachment URLs.
          Four target references were retained as textual references only.
          This compatibility entrypoint is manual-only and creates no production, merge, release, route, Pages, or external-effect authority.
          V14 remains the controlling governed HOLD.
          EOF
