# Hosted dispatch contract

The already authorized default-branch workflow must resolve and record this
branch's exact commit, run the source denominator on Ubuntu 24.04 and Windows
2025 with Node 22.16.0, and then perform two clean-root and downloaded-artifact
replays. It must preserve `runtime_binding_missing`, zero physical counts,
canonical AP-410 false, canonical G3 false, hosted repository false, and no
newly accepted gates. This branch remains unmerged.
