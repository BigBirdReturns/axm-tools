# AP-410 Platform Observation Kit v2 hosted portability input

This directory retains the exact 90,028-byte AP-410 Platform Observation Kit v2 source candidate used by the permanent default-branch hosted portability workflow. The archive is a qualification input. It is not Tools product source, runtime authority, physical evidence, canonical AP-410, or G3 authority.

The workflow recomputes the source matrix by executing `buildSourceMatrix()` and hashing its canonical JSON. It does not incorrectly search for the matrix digest as a file hash. It identifies the blocked progress record by its exact format and `progressId`, rather than by ambiguous string occurrence in other custody receipts.

The denominator requires Node 22.16.0, 49 passing contracts per execution, package verification, exact `runtime_binding_missing` standing, zero physical counts, false canonical AP-410 and G3 flags, Ubuntu 24.04 and Windows 2025, two clean roots, and a downloaded-artifact replay.
