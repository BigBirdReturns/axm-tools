# State

`IN_REVIEW`

The branch carries a deterministic bootstrap and a bounded review contract. It has no public-route authority. Promotion requires a passing exact-head browser campaign, retained rendered proofs, human inspection of the semantic feature fit, and a separate release transition.
