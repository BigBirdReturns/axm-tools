# Manzanita 99-point program status

The canonical program begins with 497 tasks. Two control tasks are closed, leaving **495 remaining tasks: 316 P0 release blockers, 168 P1 public-quality requirements, and 11 P2 enrichments**.

## Closed

### JDB99-000 · Freeze the benchmark and release law

PR #92 admitted the internal benchmark, the no-affiliation statement, the accountable design-integrator seat, the twelve weighted score dimensions, the hard failure rules, the phase exits, and the task-completion law. The benchmark does not claim review, endorsement, approval, or affiliation by Jen Darhy Bucci, Anduril, or Active Theory.

### JDB99-003 · Absorb or supersede the legacy asset register

The 361 assets from PR #89 are incorporated into the canonical 497-task master register. PR #89 is closed as superseded. Its source-provider order, live-data requirements, scale laws, overlay laws, role laws, and qualification requirements remain preserved in the master backlog.

## Active

### JDB99-001 · Archive and hash every donor and failure receipt

The first custody tranche now has an executable register, deterministic SHA-256 manifest builder, Git-blob cross-checks, an immutable guard over every file in the historical public v1.4 route, explicit claim boundaries, and a validator that refuses false closure. The register admits the current public release, the PR #89 planning donor, the PR #92 and #93 program-control records, the current Pages deployment receipt, and the located identities for the PR #90 source foundation.

The exact 41-file PR #90 workflow artifact has now been rescued before its Actions expiry. Its 8,639,908 bytes matched GitHub's recorded SHA-256, all 20 source receipts and declared payload hashes passed, and an owner-only Google Drive copy was written and read back. The retained metadata receipt records every file, source status, rights field, claim scope, and the bounded credential scan.

This task remains open. Required acquisitions still include prior releases and failed builds, an independently controlled mirror of the PR #90 artifact, complete visual goldens, playtest evidence, failure receipts, broader live-data degradation bundles, historical deployment receipts, original asset-provenance records, and standalone exports. A passing custody workflow proves that retained material and declared gaps agree; it does not reduce the remaining task count.

### JDB99-002 · Rebase and integrate the PR #90 source foundation

PR #90 has been reconstructed as one commit directly above the current production baseline. It is zero commits behind `main`, changes only the isolated `manzanita-next/` source foundation and its workflow, and leaves the historical `manzanita/` route byte-identical. GitHub reports the draft mergeable.

The qualified acquisition remains bound to pre-rebase head `416abd404a01e3dafa0a78520ef0f754e843ea77` and workflow run `31903077053`. The rebased head `3afcbe17a1c12217575aa247ab457038253491a6` does not inherit that live-run claim by assertion. Integration remains open until the rebased checks pass and the source foundation lands without being presented as a successor interface. The expiring workflow artifact is now admitted to private custody, but the rebased commit still needs its own evidence because qualification does not move across commits by assertion.

## Next admissible queue

1. Complete the open `JDB99-001` acquisition ledger and retain non-expiring copies of every admitted donor class.
2. Complete `JDB99-002` by qualifying and integrating PR #90 without modifying the historical public route.
3. `JDB99-027` activate the contained multidisciplinary review board with explicit seats, vetoes, evidence requirements, and defect disposition.
4. `JDB99-004` resolve one recognizable Manzanita form language.
5. `JDB99-005` resolve the complete typography and optical-fit system.
6. `JDB99-006` resolve equal light and dark material systems.
7. `JDB99-007` resolve the icon, symbol, line, registration, and annotation constitution.
8. `JDB99-008` resolve art direction and provenance for observed, provider, captured, authored, modeled, three-dimensional, motion, and generated assets.
9. `JDB99-009` resolve motion semantics and reduced-motion equivalents.
10. `JDB99-010` resolve content hierarchy, operational labels, help, and degraded-state copy.
11. `JDB99-011` build representative production components and compositions before whole-page assembly.
12. `JDB99-018` assemble one complete public-safe demonstration place.
13. `JDB99-020` freeze commercial-provider credentials, quota, cost, cache, rights, health, and zero-key behavior.

Whole-page production remains blocked until the design constitution is proved across representative screens. The public route remains a historical donor. No score is admitted while hard failures, unknown gates, or P0 tasks remain.

## Release threshold

A candidate becomes admissible only when every P0 task is closed, no critical or high defect remains, no applicable gate is unknown, every score dimension is at least 95, the weighted score is at least 99, the exact public endpoint passes the complete campaign, and a cold successor can operate, degrade, export, restore, and roll back the release from retained receipts.
