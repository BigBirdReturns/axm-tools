# Manzanita donor custody v2

This package reopens `JDB99-001` against the repository that actually exists after the source foundation, review board, Forkline Field, public demonstration, seven apertures, Street Glide, eight overlays, five role projections, whole experience, automated P8 floor, estate parity, internal release control, and external campaign runner were admitted.

The object is custody, not product completion. The source-custody seat may locate, hash, classify, retain, and reject evidence. The design integrator may consume only the claims carried by admitted donors. Release authority alone may close custody or promote public bytes. A cold successor is the replay standard.

`CUSTODY_CONTRACT.json` freezes authority, required classes, repo-resident scopes, the historical public-route byte guard, close law, and no-task-count boundary. `DONOR_REGISTER.json` records admitted donors and ten explicit required gaps. `INDEPENDENT_ARCHIVE_RECEIPT.json` closes only the independently controlled archive and clean readback/cold-recovery gap, leaving nine required gaps open. `OBSERVED_EXECUTION_LEDGER.json` records merged operating objects without pretending to reconstruct the lost 497-row backlog. `build_custody.py` emits a deterministic SHA-256 and Git-blob manifest. `validate_custody.py` rehashes every source, enforces the historical route, refuses false closure, and rejects invented external campaign standing. `audit_history.py` enumerates unique reachable Manzanita trees while refusing to call them releases merely because they are reachable.

The ordinary workflow is expected to return `PASS` with custody status `PARTIAL`. That means the retained repo sources and gap ledger agree. It does not close donor custody. Final closure still requires zero required gaps, archived donors, exact public endpoint and deployed rollback receipts, complete visual and playtest custody, and no attempt to mutate canonical task counts without the exact row source. The independent private archive and clean verifier replay are now retained, but they do not satisfy public endpoint, deployed rollback, or external-campaign standing.

The workflow deliberately does not invoke the corrupted backlog extractor. The constitutional recovery object remains separately governed under PR #95.

## Public maintenance transition

Signal Sheet v1.4.1 is a release-authority maintenance transaction over the already public route. The current route guard moves to the exact v1.4.1 tree and blobs, while v1.4.0 remains guarded at base commit `58c2ae1b8bfa00d65ecf5fa3a2436241547a3bfb`, route tree `846132be374b1e1da5a9444dc67da877bbb32224`, and its original file blobs. The external campaign `HOLD` remains unchanged and scoped to the unreleased `manzanita-next` successor candidate. This transition closes no custody gap, performs no external campaign, and has no canonical task-count effect.

The controlling question is whether a cold successor can recover every admitted donor, verify its exact bytes, identify who created or observed it, understand what it proves and does not prove, reproduce the rule that followed, and distinguish an honest absence from forgotten custody.

## Public photographic convergence transition

Manzanita Works v1.6.0 replaces the simplified v1.4.1 Signal Sheet route with a bounded photographic place fabric. Exact v1.4.1 bytes remain guarded at commit `750ad90f40462ab442a546bdbc2c7f02c81e2b27` and route tree `f6ea68bd4c5e07919277a099039354a5920d7b2b`. The current route is guarded at candidate commit `a763a074fab4ec8cf5fe60929a7a69c1e579385b` and route tree `d17a3d232de59bf4ab3069165abfb43291dd1ef3`. The release retains photographic donor commit `507ace9af2d2121cb93614158809ee5ff88437f2`, seven distinct aperture assets, image-registered conditions, five operational seats, the adverse-use firewall, and the Essential Attention handoff.

This transaction performs no `manzanita-next` external campaign, closes no custody gap, releases no private or physical standing, reconstructs no canonical backlog rows, and has no canonical task-count effect.
