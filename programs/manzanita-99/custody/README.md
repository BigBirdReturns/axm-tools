# Manzanita donor custody v2

This package reopens `JDB99-001` against the repository that actually exists after the source foundation, review board, Forkline Field, public demonstration, seven apertures, Street Glide, eight overlays, five role projections, whole experience, automated P8 floor, estate parity, internal release control, and external campaign runner were admitted.

The object is custody, not product completion. The source-custody seat may locate, hash, classify, retain, and reject evidence. The design integrator may consume only the claims carried by admitted donors. Release authority alone may close custody or promote public bytes. A cold successor is the replay standard.

`CUSTODY_CONTRACT.json` freezes authority, required classes, repo-resident scopes, the historical public-route byte guard, close law, and no-task-count boundary. `DONOR_REGISTER.json` records admitted donors and ten explicit required gaps. `OBSERVED_EXECUTION_LEDGER.json` records merged operating objects without pretending to reconstruct the lost 497-row backlog. `build_custody.py` emits a deterministic SHA-256 and Git-blob manifest. `validate_custody.py` rehashes every source, enforces the historical route, refuses false closure, and rejects invented external campaign standing. `audit_history.py` enumerates unique reachable Manzanita trees while refusing to call them releases merely because they are reachable.

The ordinary workflow is expected to return `PASS` with custody status `PARTIAL`. That means the retained repo sources and gap ledger agree. It does not close donor custody. Final closure requires zero required gaps, archived donors, an independently controlled archive and cold recovery, exact public endpoint and deployed rollback receipts, complete visual and playtest custody, and no attempt to mutate canonical task counts without the exact row source.

The workflow deliberately does not invoke the corrupted backlog extractor. The constitutional recovery object remains separately governed under PR #95.

The controlling question is whether a cold successor can recover every admitted donor, verify its exact bytes, identify who created or observed it, understand what it proves and does not prove, reproduce the rule that followed, and distinguish an honest absence from forgotten custody.
