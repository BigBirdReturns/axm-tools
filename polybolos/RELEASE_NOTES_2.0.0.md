# Polybolos evidence contract 2.0.0

This release corrects a denominator-substitution failure in the first deployed surface.

Mark Brown’s original transaction was presentation integration: admit his log and report, preserve provenance, normalize the trace, attribute the report’s broader claims, populate the professional surface, and make the next handoff reusable. That transaction is now recorded permanently as `COMPLETE`.

The administrator-induced fifteen-decision allowlist scenario is recorded permanently as `PASS` for the claim it exercised. The trace supports ten in-list `SMALL_UAS` authorizations, five out-of-list `MEDIUM_UAS` safe denials, zero observed mapping deviations, communications restoration, Standing Orders deactivation, and acknowledgment through sequence 15.

The later sixteen-gate framework is retained only as a prospective qualification catalog. Each optional plan freezes its claim, scope, required artifacts, procedure, pass predicates, refusal predicates, and exclusions before execution. A new plan cannot retroactively alter an earlier receipt.

The public release now carries a sanitized normalized cartridge and removes the raw producer log and report from the current public tree. Original bytes may enter a private export only after explicit local opt-in. Historical Git commits are outside the current-tree guarantee and would require a separate history-rewrite transaction for permanent purge.
