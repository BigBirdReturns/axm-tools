# AP-410 runtime-binding observation kit v2 carrier

This directory is an execution-only carrier for `AXM-Aperture-G3-Platform-Observation-Kit-v2`. It exists to prove that the exact 90,028-byte source archive reproduces its fixture-refusal and runtime-binding law on GitHub-hosted Ubuntu 24.04, Windows 2025, and two clean cold roots.

The source archive is accepted only at SHA-256 `71f4a03b50138c4f37e1fc5bce16a211f1e72f06ad5338700db1f5eaaf19bf74`. The clean package has no `RUNTIME_BINDING.json`, no physical evidence, no nonzero progress, and no authority to accept AP-410, G3, a hosted repository, publication, or any gate. `compile-progress` must remain `BLOCKED` with reason `runtime_binding_missing`.

This branch and pull request must close without merge. The retained output is the checksum-bound workflow artifact and its independent replay, not a Tools product or a future Aperture repository.
