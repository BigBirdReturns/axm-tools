# Temporary AP-403 v2 source carrier

This directory carries one checksum-bound AP-403 second-screen source candidate for cross-platform reconstruction. It exists only because `BigBirdReturns/axm-aperture` is absent. The branch and pull request must close without merge after the artifact is independently verified.

The source is stored directly under `source/aperture-target/`. Tools owns transport and qualification only. The target owns no authenticated LAN transport, certificate, key, bearer, QR authority, profile inference, provider credential, query execution, selection execution, playback execution, story state, personal history, package authority, publication authority, or gate authority.

A green workflow does not accept canonical AP-403, AP-219, G4, a native handset, television, console, closed-app client, lifecycle, accessibility, portability, publication, or the absent dedicated repository.
