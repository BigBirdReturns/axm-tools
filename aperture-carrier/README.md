# AP-403 second-screen source carrier

This directory is a temporary, never-merge transport for a future `BigBirdReturns/axm-aperture` repository. The exact eleven-file AP-403 source candidate is encoded as sixteen ordered ASCII base64 shards under `source-archive/`. Qualification concatenates and decodes those shards, verifies source archive SHA-256 `8b0324f09311833ea5cf4aabcc731f3e601d3478514703e8c95276a78be87a2c`, reconstructs every path in `TARGET_SHA256SUMS`, and runs the complete fifty-two-contract denominator.

The target consumes only external, credential-free pairing and connection projections, an AP-213-shaped read-only snapshot, and a read-only AP-401 Coach projection. It owns no pairing transport, certificate, bearer, QR authority, LAN listener, viewer inference, query execution, selection execution, playback execution, persistence, story, ledger, package, publication, or gate authority. AP-219 remains planned and unexecuted. A green carrier result does not accept AP-403, AP-219, G4, a native client, authenticated LAN transport, television or console integration, accessibility, lifecycle, or portability.

The pull request carrying this directory must close without merge after retaining its checksum-bound artifact.
