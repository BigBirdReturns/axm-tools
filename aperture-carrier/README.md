# AP-402 desktop overlay source carrier

This directory is a temporary, never-merge transport for a future `BigBirdReturns/axm-aperture` repository. The normative eleven-file target is carried as sixteen ordered ASCII base64 shards under `source-archive/`. Hosted qualification concatenates and decodes those shards, verifies the exact 16,934-byte source archive, reconstructs all target paths, executes twenty-seven contracts on Ubuntu 24.04 and Windows 2025, and on Ubuntu rebuilds the tar with normalized metadata and requires byte identity.

`TARGET_SHA256SUMS` exposes every reconstructed path and digest. `PREDECESSOR_RECEIPT.json` binds the external AP-213 policy identity and closed-unmerged AP-401 source candidate. `AP-402.json` is the target programme receipt. No generated package output is committed.

The overlay target is a pure state law. Snapshot refresh cannot open a window. User activation emits intent only. External host observation establishes visibility. Secure-surface observation and package or viewer replacement revoke visibility admission and withhold substantive content until a fresh observation and activation. The source has no pixel capture, native topmost call, transport, persistence, local clock, provider credentials, query execution, selection execution, playback execution, story, ledger, package, publication, or gate authority.

A green carrier run does not accept canonical AP-402, NFR-009, G3, the absent repository, or an operating-system overlay implementation. The pull request must close without merge after retaining the checksum-bound artifact.
