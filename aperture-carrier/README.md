# AP-405 source-candidate carrier

This temporary tree carries the AP-405 in-story selection source candidate for cross-platform qualification and deterministic cold reconstruction. It must close without merge. The source belongs at the future `BigBirdReturns/axm-aperture` repository root; Tools owns transport and qualification only.
